// JavaScript for student reports page

window.onload = function () {
  const reportsSection = document.querySelector('section.card');
  reportsSection.innerHTML = '<p>Internship reports and related content will be shown here.</p>';
};
